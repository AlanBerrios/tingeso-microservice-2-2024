apiVersion: v1
kind: PersistentVolume
metadata:
  name: creditevaluation-db-pv
  labels:
    type: local
spec:
  storageClassName: standard
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/data/creditevaluation-db-data"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: creditevaluation-db-pvc
  labels:
    app: mysql
    tier: database
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: creditevaluation-db-deployment
  labels:
    app: creditevaluation-db-deployment
    tier: database
spec:
  selector:
    matchLabels:
      app: creditevaluation-db-deployment
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: creditevaluation-db-deployment
        tier: database
    spec:
      containers:
        - name: creditevaluation-db
          image: mysql
          imagePullPolicy: "IfNotPresent"
          ports:
            - containerPort: 3306
          env:
            - name: MYSQL_CLIENT
              valueFrom:
                secretKeyRef:
                  name: mysql-secret
                  key: DB_CLIENT
            - name: MYSQL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: mysql-secret
                  key: DB_PASSWORD
            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: mysql-secret
                  key: DB_PASSWORD
          volumeMounts:
            - name: creditevaluation-db-storage
              mountPath: /var/lib/mysql
      volumes:
        - name: creditevaluation-db-storage
          persistentVolumeClaim:
            claimName: creditevaluation-db-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: creditevaluation-db-service
  labels:
    app: creditevaluation-db-service
    tier: database
spec:
  ports:
    - protocol: TCP
      port: 3306
      targetPort: 3306
  selector:
    app: creditevaluation-db-deployment
    tier: database
  type: LoadBalancer
